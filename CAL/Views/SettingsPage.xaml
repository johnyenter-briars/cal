<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="CAL.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:vm="clr-namespace:CAL.ViewModels"
    Title="{Binding Title}">

    <ContentPage.BindingContext>
        <vm:SettingsViewModel />
    </ContentPage.BindingContext>

    <Grid RowDefinitions="*,Auto">
        <ScrollView Grid.Row="0">
            <VerticalStackLayout Padding="16" Spacing="14">
                <Label Text="Settings" Style="{StaticResource TitleLabelStyle}" />

                <Border Style="{StaticResource CardBorderStyle}">
                    <VerticalStackLayout Spacing="8">
                        <Label Text="Server" Style="{StaticResource SectionLabelStyle}" />
                        <Entry x:Name="IpAddress" Text="{Binding HostName}" Placeholder="Domain name or IP address" />
                        <Entry x:Name="Port" Text="{Binding Port}" Placeholder="Port" Keyboard="Numeric" />
                        <Entry x:Name="ApiKey" Text="{Binding ApiKey}" Placeholder="API key" />
                    </VerticalStackLayout>
                </Border>

                <Border Style="{StaticResource CardBorderStyle}">
                    <VerticalStackLayout Spacing="8">
                        <Label Text="User" Style="{StaticResource SectionLabelStyle}" />
                        <Entry x:Name="UserId" Text="{Binding UserId}" Placeholder="User ID" />
                        <Entry x:Name="DefaultCalendarId" Text="{Binding DefaultCalendarId}" Placeholder="Default calendar ID" />
                    </VerticalStackLayout>
                </Border>

                <Border Style="{StaticResource CardBorderStyle}">
                    <VerticalStackLayout Spacing="8">
                        <Label Text="Notifications and Recurrence" Style="{StaticResource SectionLabelStyle}" />
                        <Entry x:Name="MaxNumTimesNotify" Text="{Binding MaxNumTimesNotify}" Placeholder="Max notifications per event" Keyboard="Numeric" />
                        <Entry x:Name="DefaultYearsToRepeat" Text="{Binding DefaultYearsToRepeat}" Placeholder="Years to repeat yearly events" Keyboard="Numeric" />
                        <Picker
                            Title="Start of week"
                            ItemsSource="{Binding DaysOfWeek}"
                            SelectedItem="{Binding DefaultStartOfWeek, Mode=TwoWay}" />
                    </VerticalStackLayout>
                </Border>
            </VerticalStackLayout>
        </ScrollView>

        <Grid Grid.Row="1" Padding="16,10" BackgroundColor="{StaticResource SurfaceColor}">
            <Button x:Name="saveChangesBtn" Command="{Binding SaveChangesCommand}" Text="Save Changes" />
        </Grid>
    </Grid>
</ContentPage>
